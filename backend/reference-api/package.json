{
  "name": "@v1/reference-api",
  "version": "0.1.0",
  "private": true,
  "description": "Reference external backend — proves the DIRECT/PROXY/WEBHOOK attach paths (swappable for your real backend).",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs"
  },
  "dependencies": {
    "jose": "^5.6.3"
  }
}