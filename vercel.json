{
  "buildCommand": "cd ../../apps/app && turbo run build --filter=@v1/app --force",
  "outputDirectory": "apps/app/.next",
  "framework": "nextjs",
  "ignoreCommand": "git log -1 --pretty=%B | grep -q \"[skip deploy]\" && echo \"skip\" || echo \"\""
}